# Acceptance-criteria map

GENERATED by `scripts/criteria-map.py` (`bun run criteria:map`). Do not edit:
the criteria are parsed from the build spec, the CI coverage is parsed from
`.github/workflows/ci.yml`, and every count below is computed. Change the
evidence table in the script, not this file.

- **19 of 28** criteria are backed by a CI job.
- **13** of those run on all three operating systems.
- **9** are NOT evidenced by CI today: 10, 11, 12, 22, 23, 24, 25, 26, 28

| # | Status | Evidence | Criterion |
|---|---|---|---|
| 1 | CI on 3 OS | verify matrix + the space-in-path job | `bun install && bun run verify` exits 0 on Windows, macOS, and Linux, from a repository... |
| 2 | CI on 3 OS | contract:check-drift, inside verify | `packages/contract` exports every schema in Section 7 with `SCHEMA_VERSION === 1`; `bun... |
| 3 | CI on 3 OS | engine determinism gate | `bun run verify:determinism` exits 0: all 5 fixtures produce identical `fingerprint` va... |
| 4 | CI on 3 OS | engine tests, inside verify | Every path in every `AnalysisResult` is repo-relative POSIX; a test asserts zero occurr... |
| 5 | CI on 3 OS | engine tests, inside verify | `AnalysisResult` contains no timestamp, no duration, no absolute path, no hostname, and... |
| 6 | CI on 3 OS | snapshot tests, inside verify | Snapshot tests exist and pass for `node-express`, `react-app`, `python-flask`, `mixed-m... |
| 7 | CI on 3 OS | kitchen-sink snapshot assertions, inside verify | `kitchen-sink`'s snapshot asserts: the known 3-file cycle appears as exactly one `Cycle... |
| 8 | CI on 1 OS | Linux-only by construction: unshare -rn | `bun run verify:no-network` exits 0: the engine bundle contains no network identifier,... |
| 9 | CI on 2 OS | Rust suite | A test asserts `tauri.conf.json`'s CSP contains no remote origin and `capabilities/defa... |
| 10 | NO GATE | coverage thresholds are not enforced anywhere | Coverage gates pass: ≥ 80% lines and branches for `contract`, `engine`, the UI, and Rus... |
| 11 | CI-BLOCKED — gate exists, not wired | bench: wall-clock budgets, deliberately off shared runners | `bun run bench` exits 0 against every budget in Section 11's performance table. |
| 12 | MANUAL | 10s cold-open on a fresh 1,000-file repo, with UI | Opening a fresh 1,000-file repo shows the overview, graph, roadmap, module map, and wor... |
| 13 | CI on 3 OS | UI tests, inside verify | With `ai.isEnabled === false`, the mode indicator reads exactly `🔒 Static mode · no net... |
| 14 | CI on 3 OS | UI tests, inside verify | With AI enabled, the indicator reads exactly `☁️ AI mode · <provider>/<model> · snippet... |
| 15 | CI on 2 OS | key-never-persisted half only; the 5s flow is manual | Adding a key takes: open Settings → paste → click "Test key" → inline success in under... |
| 16 | CI on 2 OS | redaction corpus, in the Rust suite | The redaction corpus test passes: ≥ 30 planted secrets are all replaced by `<redacted>`... |
| 17 | CI on 2 OS | payload caps in Rust; the displayed counts in UI tests | An outbound AI payload never exceeds 24 files or 98,304 bytes, and the UI displays the... |
| 18 | CI on 2 OS | citation rejection, in the Rust suite | An AI answer citing a path absent from the index is rejected wholesale with `E_AI_CITAT... |
| 19 | CI on 3 OS | UI copy tests, inside verify | All 14 named error/empty states render the exact copy from Section 10; a UI test assert... |
| 20 | CI on 3 OS | axe-core + keyboard tests, inside verify | The dependency graph is fully operable with the keyboard alone (enter, traverse by rank... |
| 21 | CI on 3 OS | reduced-motion tests, inside verify | With `prefers-reduced-motion: reduce`, no Cytoscape animation and no CSS transition runs. |
| 22 | CI-BLOCKED — gate exists, not wired | e2e: wdio fixed-port race makes it unfit as a per-push gate | `bun run e2e` passes on Windows and Linux; `docs/MACOS_SMOKE.md` is signed off manually. |
| 23 | CI-BLOCKED — gate exists, not wired | installers: no release job yet; see the darwin cross-compile entry | Installers build and launch on all three platforms: `.dmg` (x64 + aarch64), `.exe` + `.... |
| 24 | NOT MET — missing docs/INSTALL.md, docs/SIGNING.md | docs | `docs/INSTALL.md` documents the exact Gatekeeper and SmartScreen bypass steps for unsig... |
| 25 | NOT MET — missing README.md | docs | `README.md` covers install, the privacy model, and how to optionally enable AI; `docs/V... |
| 26 | MANUAL | audit is PARTIAL by its own §6 until a fail-open-aware re-audit | `docs/SECURITY_AUDIT.md` shows zero open CRITICAL and zero open HIGH findings. |
| 27 | CI on 3 OS | lint + lint:check-rules-fire, inside verify; clippy in the Rust job | No source file exceeds 800 lines, no function exceeds 50 lines, and no block nests deep... |
| 28 | NO GATE | conventional-commit linting is not wired | Every commit follows `<type>: <description>`; CI rejects a non-conforming message. |

## What 'CI on N OS' means

A job in `ci.yml` invokes the gate, and its matrix names N operating systems.
It does NOT mean the job is currently passing — that is what the run itself
says. It means the evidence is reachable without a human.
