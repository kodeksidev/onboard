{
  "name": "onboard",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "verify": "bun run verify:js && bun run verify:rust",
    "verify:js": "bun run typecheck && bun run lint && bun run lint:check-rules-fire && bun run contract:check-drift && bun run docs:check && bun run ci:check-installs && bun run ci:check-python && bun run fixtures:check-tracked && bun run engine:check-globals && bun run criteria:check-drift && bun run ci:check-verify-coverage && bun run test",
    "verify:rust": "cargo fmt --manifest-path \"apps/desktop/src-tauri/Cargo.toml\" --all --check && cargo clippy --manifest-path \"apps/desktop/src-tauri/Cargo.toml\" --all-targets --all-features -- -D warnings && cargo test --manifest-path \"apps/desktop/src-tauri/Cargo.toml\" --no-fail-fast",
    "contract:emit": "bun run --cwd \"packages/contract\" contract:emit",
    "contract:check-drift": "bun run --cwd \"packages/contract\" contract:check-drift",
    "fixture:emit": "bun run --cwd \"packages/contract\" fixture:emit",
    "build:sidecar": "bun run --cwd \"packages/engine\" build:sidecar",
    "stage:sidecar": "bun run scripts/stage-sidecar.ts",
    "typecheck": "bun run --cwd \"packages/contract\" typecheck && bun run --cwd \"packages/engine\" typecheck && bun run --cwd \"apps/desktop\" typecheck",
    "lint": "eslint .",
    "format": "prettier --write .",
    "lint:check-rules-fire": "bun run scripts/lint-rules-fire-check.ts",
    "test": "bun run --cwd \"packages/contract\" test && bun run --cwd \"packages/engine\" test && bun run --cwd \"apps/desktop\" test",
    "docs:check": "python scripts/docs-check.py",
    "ci:check-installs": "python scripts/ci-install-check.py",
    "ci:check-python": "python scripts/ci-python-check.py",
    "fixtures:check-tracked": "python scripts/fixtures-tracked-check.py",
    "criteria:map": "python scripts/criteria-map.py",
    "criteria:check-drift": "python scripts/criteria-map.py --check",
    "verify:report": "bun run scripts/run-verify-stages.ts verify",
    "verify:report:js": "bun run scripts/run-verify-stages.ts verify:js",
    "ci:check-verify-coverage": "python scripts/ci-verify-coverage-check.py",
    "engine:check-globals": "python scripts/engine-globals-scan.py",
    "guards:scan": "python scripts/guard-disposition-scan.py"
  },
  "overrides": {
    "brace-expansion": ">=5.0.8",
    "serialize-javascript": ">=7.0.7"
  },
  "devDependencies": {
    "@eslint/js": "~10.0.1",
    "@typescript-eslint/eslint-plugin": "~8.65.0",
    "@typescript-eslint/parser": "~8.65.0",
    "@types/node": "~22.20.0",
    "bun-types": "~1.3.14",
    "eslint": "~10.8.0",
    "prettier": "~3.9.6",
    "typescript": "~5.7.3",
    "typescript-eslint": "~8.65.0"
  }
}
