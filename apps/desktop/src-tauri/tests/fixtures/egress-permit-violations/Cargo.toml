[package]
name = "egress-permit-violations-fixture"
version = "0.0.0"
edition = "2021"
publish = false

# Deliberately NOT part of any [workspace] — mirrors
# tests/fixtures/redaction-violations/. Every binary here is meant to fail
# to compile; see tests/ai_permit_compile_fail.rs.

[dependencies]
onboard = { path = "../../.." }
serde_json = "1"
